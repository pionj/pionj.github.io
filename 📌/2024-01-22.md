---
type: daily note
draft: true
---

# 2024-01-22 (January | Monday)

질문내용 : 
```
안녕하세요, 좋은 글입니다! 
1. `api.createRequest('subtract', 11, 4).call()` 호출 할 때, RPC 서버를 만들어놓고 TCP 소켓 서버로 보낸건 리퀘스트 헤더랑 바디를 보기 위해서 였나요?
2. `JSON-RPC는 요청 시 프로시저 이름, 파라미터의 위치가 정의되어 있기 때문에 어플리케이션 단에서 프로시저 이름이 있는지 없는지 검사하지 않아도 됩니다.` 이 내용이 잘 이해가 안되는데, RPC 서버를 만들 때 어플리케이션 단에서 프로시저 이름을 찾는 코드가 있었나요? 못찾겠습니다 ㅠㅠ
3. HTTP/2 는 기본적으로 TLS 를 사용한다고 하셨는데요, 인증서가 없으면 안되는걸로 알고 있습니다. 그래서 혹시`grpc.ServerCredentials.createInsecure()` 이런 옵션을 주신건가요?

감사합니다!
```

